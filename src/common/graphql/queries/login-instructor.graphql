query LoginInstructor($email: String!, $password: String!) {
    loginInstructor(email: $email, password: $password) {
        id
        email
        name
        token
        organizations {
            id
        }
    }
}
